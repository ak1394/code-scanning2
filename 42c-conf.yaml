audit:
  branches:
    master:
      fail_on:
        score:
          data: 60
        issue_id:
          - v3-global-http-clear
          - v3-response-schema-undefined
    dev-foo:
      discovery:
        search:
          - '**/*.json'
          - '**/*.yaml'
          - '**/*.yml'
          - '!foo/**'
      fail_on:
        score:
          data: 60
        issue_id:
          - v3-global-http-clear
          - v3-response-schema-undefined
